# AppCache Test Application

This is a simple html/js application used to test AppCache behaviour in the browser. I built it to ensure that AppCache does work properly in `WKWebView` in an iOS application.